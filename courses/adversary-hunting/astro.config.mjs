import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://moonbaseiverson.github.io',
  base: '/courses/adversary-hunting',
  outDir: '../../dist/courses/adversary-hunting'
});
